[project]
name = "sdk-game"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["foxglove-sdk>=0.7.1", "watchfiles>=1.0.5"]

[dependency-groups]
dev = [
    "ruff>=0.11.8",
]
