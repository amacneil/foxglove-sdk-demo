[project]
name = "sdk-game"
version = "0.1.0"
description = ""
authors = [
    {name = "Adrian Macneil",email = "adrian@foxglove.dev"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "foxglove-sdk (>=0.7.1,<0.8.0)",
    "watchfiles>=0.21.0"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
